<h1>Edit Post</h1>

<%= form_for @post, :url => admin_board_post_path(@board, @post), :html => { :mulitpart => true } do |f| %>
  <%= render "form", :f => f, :post => @post %>
<% end %>

<%= link_to("Back", board_path(@board)) %>
