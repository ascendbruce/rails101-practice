<h1>New Post</h1>

<%= form_for @post, :url => board_posts_path(@board), :html => { :mulitpart => true } do |f| %>
  <%= render "form", :f => f, :post => @post %>
<% end %>

<%= link_to("Back", board_path(@board)) %>
