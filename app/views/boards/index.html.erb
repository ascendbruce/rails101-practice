<h1>Boards</h1>

<table>
  <tr>
    <th>name</th>
  </tr>
  <% @boards.each do |board| %>
    <tr>
      <td><%= link_to(board.name, board_path(board)) %></td>
    </tr>
  <% end %>
</table>

<%= will_paginate @boards %>
